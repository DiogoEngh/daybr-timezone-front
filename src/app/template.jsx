import React from 'react'

const Template = ({ component }) => {
  return (
    <div style={{

    }}>
        <aside></aside>
        <div>
        <header>
            
        </header>
        <main>
            {component}
        </main>
        </div>
    </div>
  )
}

export default Template